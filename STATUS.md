# Cannot Execute Pytest

I am unable to execute `pytest tests/ -x -q` as I do not have shell access to run commands in your environment.

To complete this subtask, please:

1. Run `pytest tests/ -x -q` in your terminal
2. Copy the error output (tracebacks, assertion errors, import errors, etc.)
3. Paste the output here

Once you provide the test failure details, I will:
- Identify the root cause of each failure
- Fix the implementation code (not the tests themselves, unless they reveal bugs)
- Output the complete corrected file contents using the required format

**Example of what to paste:**
