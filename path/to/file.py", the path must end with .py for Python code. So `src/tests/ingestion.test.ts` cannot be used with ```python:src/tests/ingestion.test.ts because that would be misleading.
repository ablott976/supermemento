I think the user made an error and the file should be `tests/test_ingestion.py` or similar. But I must follow the user's target files.

Let me reconsider. Maybe the project has both TypeScript and Python, and the subtask is specifically about the Python tests, but the user listed both files by mistake? Or maybe I should only output the Python file content for `tests/test_services.py`?

But the subtask says "Write tests for the new filtered vector search functionality" and mentions both files.

Given the constraint that I must use the format
